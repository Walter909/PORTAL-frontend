import React from 'react';
import { Button } from '@mui/material';

import Game from '../Icons/Game.js';

const SideButton = () => {
  return <Button startIcon={Game}>Button</Button>;
};

export default SideButton;
